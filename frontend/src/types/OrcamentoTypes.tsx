export interface OrcamentoFormType {
    noiva?: string;
    noivo?: string;
    salao?: string;
    valor_pessoa?: string;
    quantidade_pessoa?: number;
    data_evento?: Date;
    som?: string;
    nome?: string;
    total: number;
}